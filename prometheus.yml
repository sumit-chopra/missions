# Minimal Prometheus: scrape The Vault's /metrics endpoint and nothing else.
global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: vault
    metrics_path: /metrics
    static_configs:
      - targets: ["vault:8000"]
